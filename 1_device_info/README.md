# OBJECTIVE: to fetch the device model and Android version


## ADB Commands Used

adb devices **to view attached device**
adb shell getprop ro.product.model **gets device model**
>adb shell getprop ro.build.model **gets android version**


## Output

RMX3761
13